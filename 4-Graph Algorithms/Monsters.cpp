#include <bits/stdc++.h>

using namespace std;


void dfs(string& pat,vector<string>& mp){
    
}

int main(){
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int n,m;
    vector<string> mp;
    string str;
    for(int i = 0; i<m; ++i){
        cin>>str;
        mp.push_back(str);
    }



}
